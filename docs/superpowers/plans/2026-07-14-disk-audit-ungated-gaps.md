# Disk Audit Ungated Gaps Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Close six safe disk-audit gaps without cleanup, deletion, or changes outside disk_magician.

**Architecture:** Extend the existing observer and inventory scripts in place, exercising external commands through their current bounded subprocess boundary. Keep deployment configuration in the existing observer launchd template, package mirrors generated by `sync_package_tree.sh`, and harness/documentation corrections in their existing files.

**Tech Stack:** Python 3 stdlib, Bash, ripgrep, GitHub CLI REST API, launchd, uv.

## Global Constraints

- Base branch is `origin/main` at `6bb839cc11d4eb68d91be55975134fa2e1f72a7c`.
- Do not delete or clean anything; do not touch ez-gh-actions or WorldArchitect.
- Write failing tests before production changes.
- Package changes require a version bump, package-tree sync, forced uv reinstall, and deployed-copy parity.
- Commit and push each green unit.

---

### Task 1: Package behavior and deployment

**Files:**
- Modify: `scripts/disk_observer.py`
- Modify: `tests/test_disk_observer.py`
- Modify: `launchd/com.jleechanorg.disk-magician-observer.plist.template`
- Modify: `scripts/disk_inventory.py`
- Modify: `tests/test_disk_inventory.py`
- Modify: `pyproject.toml`
- Generated mirrors: `src/disk_magician/**`

**Interfaces:**
- Observer Docker commands inherit a resolved Colima `DOCKER_HOST` when no explicit host is set.
- `_git_facts()` returns bounded PR-attribution state and inventory eligibility fails closed when it is incomplete.
- `_ao_reference_map()` uses one bounded native fixed-string scan and returns `(references, complete)`.
- Unknown caches include a non-empty `upgrade_path`.

- [ ] Add observer and inventory regression tests and verify RED.
- [ ] Implement the smallest shared-function changes and verify GREEN.
- [ ] Bump the package version and run `scripts/sync_package_tree.sh`.
- [ ] Run targeted and safety suites; commit and push.
- [ ] Force-reinstall the uv tool, install only the observer plist, and verify deployed hashes plus a live Docker-positive observer sample.

### Task 2: Concurrency-safe pressure-sweep harness

**Files:**
- Modify: `tests/test_pressure_sweep.sh`

**Interfaces:**
- The test executes a temp copy of `pressure_sweep.sh` beside its mocks and never writes repository cleanup scripts.

- [ ] Verify the existing real-script copy pattern is present (RED safety check).
- [ ] Replace it with a temp-script fixture.
- [ ] Run the test and static no-overwrite check; commit and push.

### Task 3: Correct the canonical evidence interval

**Files:**
- Modify: `roadmap/2026-07-14-disk-audit-bead-execution-goal.md`

**Interfaces:**
- The roadmap distinguishes the `01:07:45Z→01:08:45Z` VM PID transition from the `01:08:45Z→01:09:45Z` allocation recovery.

- [ ] Recheck the raw observer timestamps.
- [ ] Correct only the affected sentence and verify exact timestamp text.
- [ ] Commit and push.

### Task 4: Final verification

- [ ] Run all targeted Python and shell suites plus package sync.
- [ ] Confirm branch tracking and remote commit URLs.
- [ ] Confirm no cleanup/deletion commands ran and report any remaining blocker.
