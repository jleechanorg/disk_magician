/up changelog — 2026-07-30 23:18 PDT

Persistence call: "disk-investigation methodology must (1) ALWAYS find the
last-week floor before proposing anything; (2) ALWAYS pull the
granularity_buckets from the git-backed ledger and present per-path
before/after deltas vs the floor snapshot."

Result of legacy /up invocation (this change came FIRST, before the skill
itself was updated to default to repo scope):

| Surface | Status | Note |
|---|---|---|
| `<repo>/CLAUDE.md` | ✅ full canonical copy | Applied through AGENTS.md → CLAUDE.md symlink (single edit, no double-write). New section "Investigation methodology" at line 3. |
| `~/.claude/CLAUDE.md` | ✅ one-line pointer | At line 171 inside "Disk diagnosis — three concurrent lanes". Names the canonical owner, no rule-body duplication. |
| `~/.codex/AGENTS.md` | ✅ one-line pointer | At line 86, same rule spot as Claude. |
| `<repo>/AGENTS.md` | ✅ no-touch | Symlink to CLAUDE.md; edited the target, link resolves correctly. |
| `<repo>/README.md` | ✅ untouched | No behavioral rule belongs here — user-facing only. |
| Gemini / Cursor | ✅ untouched | CLAUDE.md pointer covers cross-runtime reach. |

Backups: `AGENTS.md.bak-2026-07-30-230706` (pre-symlink), `CLAUDE.md.bak-2026-07-30-230721`, `~/.claude/CLAUDE.md.bak-2026-07-30-230706`, `~/.codex/AGENTS.md.bak-2026-07-30-230706`.

Conflict check: `grep -c 'Find the last-week floor'` on `~/.claude/CLAUDE.md`
and `~/.codex/AGENTS.md` = 1 each (pointer only); the full procedural body
lives in one file only.

---

Skill update (later this turn, by the same `/up` follow-up): added `--repo`
default + brief Markdown contract. New /up invocation will write to
`<active-repo>/CLAUDE.md` by default and produce a `roadmap/up-changelog-*.md`
table like this one instead of dumping the receipt into chat.

---

Research artifact for the 296 GiB residual question lives at
`roadmap/research-residual-296gib-20260730.md` (synthesized from three
direct-subagent research sweeps A/B/C). Bottom line: APFS snapshots ruled
out as a residual source; the realistic operator unlock is **Full Disk
Access grant + the Library/MobileSync/Photos probe sequence**. See that
file before any further "200 GiB does not exist" reasoning.
