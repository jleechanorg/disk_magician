# Disk Reclaim Plan — jeffreys-macbook-pro Data Volume — 700G target

**Generated:** 2026-07-17, via `/swarm` (7 parallel sonnet miners + 1 synthesis pass, Workflow tool, run `wf_aeb6805a-dd5`). Goal: reduce Data volume usage from 821.2 GiB to 700 GiB (reclaim 121.2 GiB).

**⚠️ Security finding, independent of disk cleanup — action immediately:** the `duplicate-repos` miner found 3 live-looking GitHub PATs embedded in plaintext in `.git/config` remote URLs:
- `/Users/jleechan/projects/worldarchitect.ai` → `ghp_PU2m...REDACTED...a2DziPx (rotate immediately, see issue #27)`
- `/Users/jleechan/projects_reference/jleechanclaw` → `ghp_T9mz...REDACTED...c4ocDX03MDnq (rotate immediately, see issue #27)`
- `/Users/jleechan/repos/jleechanclaw` → a GitHub PAT (value redacted, rotate immediately — see issue #27)

Rotate/revoke these regardless of what happens with the cleanup plan below.

---

## 1. Executive Summary

| Metric | Value |
|---|---:|
| Current Data volume used | **821.2 GiB** |
| Target | **700 GiB** |
| Gap to close | **121.2 GiB** |

**The gap is closeable — but not from SAFE items alone.**

- **SAFE-only tier total: ≈94.56 GiB** — falls **26.64 GiB short** of the 121.2 GiB target.
- **SAFE + REVIEW tier total: ≈192.6 GiB** — comfortably closes the gap, with ~71.4 GiB of margin.
- The single highest-leverage lever is the **41 GiB of stale `.claude/worktrees/` checkouts inside `~/projects/worldarchitect.ai`** (to be swept via this repo's own `worktree_hygiene.sh`, not a blind `rm`). Adding just this one REVIEW item to the SAFE tier (94.56 + 41 = **135.56 GiB**) already exceeds the 121.2 GiB target **without** touching Python venvs, ambiguous duplicate-repo groups, or any of the large-files REVIEW items.
- **Unclosed/unmeasurable lever:** `/private/var/dirs_cleaner` (claimed ~165 GiB in prior investigation) remains **completely unverified** — blocked by macOS Launch Constraints (SIGKILL on direct exec, confirmed via a real crash report) and no readable CLI tool exposes purgeable/path-level space for it. Disclosed honestly as a gap, not counted anywhere in the math below.
- **Unit-reconciliation note:** `diskutil apfs list` (Capacity Consumed 882.5 GB ≈ 822.0 GiB) roughly matches the 821.2 GiB baseline, but `system_profiler SPStorageDataType` implies ~870.2 GiB used — ~48 GiB higher. Two independent read-only tools disagree by that margin; not resolved here.

**Recommendation:** Execute the full SAFE tier (11 items, ≈94.56 GiB), then execute the single largest REVIEW item (`worktree_hygiene.sh` sweep of `~/projects/worldarchitect.ai/.claude/worktrees/`, ≈41 GiB) under explicit user approval. That combination (≈135.56 GiB) closes the 121.2 GiB gap with margin.

---

## 2. Ranked Action Table

### SAFE tier (sorted by GiB descending)

| Item / path | Est. GiB | Confidence | Rationale |
|---|---:|---|---|
| `node_modules` dirs (882 found, homedir-wide) | 41.31 | SAFE | `npm`/`pnpm install` rebuilds from lockfile — HIGH regen confidence |
| Colima datadisk `fstrim` (active "default" profile) | ~35.75 | SAFE | Guest ext4 shows 3.5 GiB used vs 39.25 GiB host-allocated sparse file; `fstrim` is non-destructive |
| Duplicate git clones, high-confidence redundant copies | 6.02 | SAFE | 14 groups where a clean, up-to-date, matching-origin copy exists elsewhere with 0 unique commits |
| `~/Library/Application Support/Google/Chrome/OptGuideOnDeviceModel/2025.8.8.1141/weights.bin` | 4.0 | SAFE | Disposable Chrome on-device AI model weights, redownloads on demand |
| `~/.npm` | 3.5 | SAFE | npm registry cache |
| `~/.cache` (codex-runtimes, uv, gh, fastembed, node caches) | 2.2 | SAFE | All auto-redownload |
| `~/Library/Application Support/Google/GoogleUpdater/crx_cache/618a6d0f...` | 0.69 | SAFE | Chrome component-updater cache blob, disposable |
| `~/Downloads/voyage_gameplay_stream/Devs Play Voyage...mp4` | 0.64 | SAFE | Ordinary downloaded media, no other reference |
| `.worktrees/*` STALE-LIKELY-SAFE (10 entries) | 0.27 | SAFE | Verified 0 uncommitted changes; ≥1000 commits behind or 30+ days stale |
| `~/.cargo/registry` | 0.10 | SAFE | cargo redownloads crates |
| `.pytest_cache` dirs (111 found) | 0.08 | SAFE | pytest recreates on next test run |
| **SAFE subtotal** | **≈94.56** | | |

### REVIEW tier (sorted by GiB descending — needs explicit user approval before touching)

| Item / path | Est. GiB | Confidence | Rationale |
|---|---:|---|---|
| `~/projects/worldarchitect.ai/.claude/worktrees/` (131 stale worktree checkouts) | 41 | REVIEW | Must route through `worktree_hygiene.sh`, not a blind `rm` — parent repo is the active daily-driver clone |
| Python venvs, unreferenced (102 of 104 found; 2 excluded as actively referenced) | 32.98 | REVIEW | MEDIUM regen confidence; includes ~15+ pure stale `venv.bak.*` backup dirs needing no rebuild at all |
| `~/Library/Application Support/Cursor/User/globalStorage/state.vscdb` | 3.2 | REVIEW | Real Cursor chat/workspace-history DB — prune via Cursor's own storage-cleanup UI first |
| `~/.lima/colima/diffdisk` (orphaned, stopped, non-active lima instance) | 3.9 | REVIEW | Not open by any process; confirm truly unused before `limactl delete` |
| `~/Library/Caches` — other app caches (30+ subdirs) | 3.36 | REVIEW | Mixed HIGH/MEDIUM regen confidence |
| Duplicate-repo extra: 3 non-primary `worldarchitect.ai` copies | 1.96 | REVIEW | Mixed dirty/behind state; needs verification before removal |
| `~/.colima/_lima/_disks/colima-ci/datadisk` | 2.1 | REVIEW | Docker data-root for stopped "ci" profile — recreatable |
| Duplicate-repo extra: `projects_reference/jleechanclaw` (worst copy) | 1.84 | REVIEW | Miner called this "safe" but it's actually dirty + contains the embedded PAT above — flagged discrepancy, manual verification needed |
| Duplicate-repo ambiguous groups (smartclaw/worldai_claw/worldai_archive) | ~1.5 | REVIEW | Likely legitimate parallel in-flight PR branches — check PR status before touching |
| Antigravity stale task logs (`task-618.log` + `task-224.log`) | 2.32 | REVIEW | 10–11 days old, no antigravity process running |
| `~/.colima/_lima/colima-ci/diffdisk` | 1.0 | REVIEW | Overlay disk, stopped "ci" profile, recreatable |
| `~/.local/share/opencode/opencode.db` | 0.67 | REVIEW | App data, 4d idle, no process running |
| `~/.lima/colima/basedisk` | 0.82 | REVIEW | Base VM image for the same orphaned instance above |
| `~/.config/mcp-daemon/logs/context7.log` | ~0.9 | REVIEW | Actively growing right now; safe to truncate |
| `~/projects/user_scope/backup/.../mcp-daemon/logs/context7.log` (committed copy) | 0.53 | REVIEW | Checked into `user_scope` backup repo; needs `git rm --cached` + commit |
| **REVIEW subtotal** | **≈98.04** | | |

### DO-NOT-TOUCH tier (listed for completeness — excluded from all reclaim math)

| Item / path | Est. GiB | Rationale |
|---|---:|---|
| `~/.hermes/state.db` | 6.7 | Active production Hermes gateway DB |
| `~/.colima/_lima/_disks/colima/datadisk` (active default profile) | 39* | *Covered above via `fstrim` under SAFE — the file itself must not be `rm`'d, only trimmed |
| WhatsApp `Media.tar` (iCloud-synced backup) | 2.8 | Real personal backup data |
| `~/.codex/logs_2.sqlite` | 3.0 | Active Codex logs DB |
| `~/.codex/state_5.sqlite` | 2.5 | Active Codex session-state DB |
| `.worktrees/*` STILL-ACTIVE (96 entries) | 17.14 | Has uncommitted changes and/or recent activity |
| `~/Library/Application Support/FileProvider/.../database/db` | 1.8 | Open by live `fileproviderd` |
| `~/.codex/sessions/.../rollout-...019cfc68....jsonl` | 1.2 | Standing "never delete Codex sessions" rule |
| `~/.colima/_lima/colima/diffdisk` (active) | 1.5 | In-use VM overlay disk |
| `~/.cmuxterm/workstream.jsonl` | 1.0 | Actively-written session log for the tool running this task |
| `~/.dropbox/instance1/sync_fp/nucleus.sqlite3` | 1.0 | Active Dropbox sync-engine DB |
| `~/.codex/logs_2.sqlite-wal` | 0.84 | Active DB journal |
| `~/.codex/sessions/.../rollout-...019cfba4....jsonl` | 0.6 | Same session-preservation rule |
| `~/Library/Metadata/CoreSpotlight/.../live.2.indexArrays` | 0.58 | macOS-managed Spotlight index shard |

### UNMEASURABLE

| Item / path | Est. GiB | Rationale |
|---|---:|---|
| `/private/var/dirs_cleaner` | ~165 (unconfirmed) | Blocked at exec layer by macOS Launch Constraints (SIGKILL/crash report confirmed); no CLI exposes path-level or purgeable-space breakdown |

---

## 3. Cumulative Math

| Scenario | Running total (GiB) | vs. 121.2 GiB target |
|---|---:|---|
| SAFE only | 94.56 | **Falls short by 26.64 GiB** |
| SAFE + single largest REVIEW item (worktree_hygiene.sh sweep, 41 GiB) | 135.56 | **Closes the gap**, +14.36 GiB margin |
| SAFE + full REVIEW tier | 192.60 | **Closes the gap comfortably**, +71.4 GiB margin |

---

## 4. Execution Order Recommendation

**Phase 1 — SAFE tier:**

1. **Colima `fstrim`** (~35.75 GiB, highest single-command yield):
   ```
   colima stop && colima start
   colima ssh -- sudo fstrim -av
   ```
2. **`node_modules` bulk removal** (41.31 GiB) — 882 dirs; note 6 identical 775M `agent-orchestrator-ts` copies as a consolidation candidate. Verify no dev server has any open (`lsof`) before removing.
3. **Duplicate git clone removal, high-confidence groups** (6.02 GiB) — 14 groups, each with a verified clean/up-to-date canonical copy elsewhere.
4. **Misc large SAFE files** (5.33 GiB) — Chrome `weights.bin`, `GoogleUpdater/crx_cache` blob, Downloads gameplay mp4.
5. **Cache dirs** (5.88 GiB) — `~/.npm`, `~/.cache`, `~/.cargo/registry`, `.pytest_cache` dirs.
6. **STALE-LIKELY-SAFE worktrees** (0.27 GiB) — `git worktree remove` for the 10 verified-0-uncommitted paths, then `git worktree prune`.

**Phase 1 subtotal: ≈94.56 GiB — gap still open by 26.64 GiB.**

**Phase 2 — the one REVIEW item needed to close the gap (requires explicit user approval, touches the active daily-driver `worldarchitect.ai` clone):**

7. **`worktree_hygiene.sh` sweep of `~/projects/worldarchitect.ai/.claude/worktrees/`** (41 GiB, 131 checkouts) — dry-run first, review output, execute with `--execute` only after sign-off. Combined with Phase 1: 135.56 GiB total, +14.36 GiB margin.

**Items requiring explicit user approval before ANY action:**
- Phase 2 worktree_hygiene.sh sweep (41 GiB)
- Python venvs (32.98 GiB) — separate pure `venv.bak.*` stale backups (no rebuild needed) from actively-referenced-adjacent venvs first
- Cursor `state.vscdb` (3.2 GiB) — prune via Cursor's own UI
- Orphaned lima "colima" instance basedisk+diffdisk (4.72 GiB) — confirm via `limactl list` before delete
- Duplicate-repo extras with embedded PATs or dirty state (1.84 GiB) — rotate PAT first, then re-verify no unique commits
- Ambiguous duplicate-repo groups (~1.5 GiB) — check in-flight PR status per branch first
- Antigravity/opencode/context7.log REVIEW items (~4.8 GiB) — brief individual check recommended

---

## 5. Known Unmeasurable / Unclosed Items

- **`/private/var/dirs_cleaner` (~165 GiB claimed previously) remains completely unverified.** Blocked at the exec layer by macOS Launch Constraints — a real crash report (`~/Library/Logs/DiagnosticReports/Retired/dirs_cleaner-2026-07-16-125636.ips`, SIGKILL/"Launch Constraint Violation") shows direct invocation is killed before it can open the path. Neither `system_profiler` nor `diskutil apfs list` expose a path-level breakdown. No periodic/cron decay mechanism found. Only remaining avenue: Apple's own Storage Management system-settings pane (outside this session's capability).
- **Colima total size has a ~4.2 GiB spread across 3 independent miner measurements** (44.69 / ~45 / 43.6 / 40.78 GiB) — likely measurement-timing variance during an actively-growing disk, not resolved to one number.
- **`.worktrees/*` total has a minor 0.31 GiB discrepancy** between whole-dir `du` (17.10 GiB) and the per-entry sum (17.41 GiB) — noted, not reconciled.
- **`~/.gemini` (23.18 GiB)** — only 2.32 GiB of it (Antigravity task logs) was broken down; the remaining ~21 GiB is unaccounted for in this plan.

---

## Execution log — 2026-07-17 (autonomous, user away 12h, "no questions" authorization)

Starting point for this execution pass: 837.0 GiB used (post the earlier Colima regrowth). Root-caused Colima's growth beyond the trim guard (see `roadmap/activity/2026-07-17.md`) — fixed via `ez-gh-actions@688a798` (guest-native fstrim every 5min + `--all` scope), reclaiming 45.3 GiB on its own (837.0 → 791.7 GiB).

Executed Phase 1 SAFE tier + the high-confidence portion of the duplicate-repo REVIEW items:
- Duplicate git clone removal (18 paths, ~6-10 GiB)
- npm/cache/cargo/pytest_cache clearing
- 3 misc large SAFE files (Chrome weights.bin, GoogleUpdater cache blob, Downloads gameplay mp4)
- `node_modules` bulk removal (~800 project dirs)
- 10 STALE-LIKELY-SAFE worktrees removed via `git worktree remove`

**Two incidents during execution (both filed as beads, both investigated for actual damage):**
1. `jleechan-bdn8` — the `node_modules` enumeration wasn't scoped to exclude `~/.nvm/versions/*/lib/node_modules` (npm's own global-package directory, not a per-project cache). Caught mid-delete via a live process's command line, killed immediately. 24 broken bin symlinks resulted; 16 packages reinstalled via `npm install -g` and verified working (tsc/tsx/playwright/codex/claude-code all smoke-tested). 2 packages (`chrome-mcp-server`, `superpowers-chrome-mcp`) could not be safely restored — they were originally installed from git/tmp sources, and the public registry has an unrelated package under the same name for the former. `tsserver` also remains absent, but for an unrelated reason: the current `typescript@7.0.2` no longer ships it at all (upstream change, not caused by this incident).
2. `jleechan-jdt2` — a `git worktree remove || rm -rf` fallback pattern blindly deleted `.worktrees/jleechanclaw/browserclaw` after `git worktree remove` correctly refused with `fatal: is a main working tree` (a standalone repo, not a linked worktree, despite the directory naming convention). Investigated: 0 uncommitted changes existed, GitHub has the authoritative `jleechanorg/browserclaw` repo (pushed as recently as 2026-07-09), and 2 other local clones survive with far more history (20 ahead/0 behind, and 7 ahead/1 behind origin/main) than this copy's claimed "2 ahead, 3 months stale." Real loss is very likely limited to ≤2 small never-pushed commits — low materiality, but the process itself was wrong and is documented for future runs.

## Provenance

- Workflow run `wf_aeb6805a-dd5`, 8 agents (7 miners + 1 synthesis), 733,332 subagent tokens, 168 tool calls, ~29 min wall-clock.
- Full miner reports: `roadmap/evidence/disk_700g_miner_summary_20260717.md` (this repo).
- Baseline measurement: `df -kP /System/Volumes/Data` at time of `/swarm` invocation, 821.2 GiB used / 56.7 GiB free / 926.4 GiB total.
