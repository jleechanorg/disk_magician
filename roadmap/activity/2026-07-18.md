# Activity — 2026-07-18

- **ez-gh-actions disk-churn reduction, Phase 2 (per-runner workspace mount).** Added `RunnerConfig.workspace_host_path`, mounting a per-runner-name subdir RW at `/home/runner/_work`, wiped before every container start for job isolation. Empirically verified uid 1001 write access through the virtiofs mount before implementing. 3 new tests, 317/317 suite. Full production deployment + live verification (real `docker inspect`, real read-write as the runner user, real remove/respawn wipe-cycle proof) — hit and fixed a real deployment incident along the way: `cp`-replacing the live binary broke macOS code-signing provenance and crash-looped the daemon under SIGKILL (`codesign --sign - --force` fixed it). Commits `d43e941`/`0dd6ee5` on `jleechanorg/ez-gh-actions`.
- **200GiB disk-cleanup identification pass** (`/swarm`, 650 agents, ultracode Workflow tool, run `wf_5aaa388d-23e`). Produced `roadmap/2026-07-18-plan-200g-target-v2.md` (supersedes the 2026-07-17 700G-target plan), 123 deduplicated candidates all ≤5GiB, 53.24 GiB total identified (short of the 200GiB ask, honestly disclosed why). Found and locally remediated 2 new live GitHub PATs embedded in `/private/tmp` gist-evidence scratch clones (deleted the clones, tokens themselves still need user-side rotation — bead `jleechan-s99f`, P0). Confirmed the original 3 previously-flagged PATs are already rotated/clean.
- **Executed the SAFE tier, conservatively.** 15.17 GiB verified reclaimed (`df -kP /System/Volumes/Data`: 731.75→716.58 GiB used). Excluded 4 Antigravity task-log files (2.59 GiB) over an LLM-conversation-retention concern, and skipped the entire `.ao/data/worktrees/*-auto-export*` category after direct pre-delete verification found the report badly wrong there (bead `jleechan-6mu5`, tracks fixing the report-generation process itself). Caught a false-positive "9000+ commits ahead" signal on 3 `.codex/worktrees` entries (rewritten-history artifact on worldarchitect.ai's main branch) — verified each via `gh api .../pulls/<n>` before removing. `roadmap/2026-07-18-plan-200g-target-v2.md`'s Execution log section has the full step-by-step.
- **Re-confirmed a real tool gap**: `disk-magician audit`'s top-down lane exhausts its 480s wall-clock cap walking `/System/Volumes/Data/Library/*` (system-wide) before ever reaching `/Users` when rooted at the whole Data volume — filed `jleechan-ez97` (distinct from the already-closed node-budget bug `jleechan-sdlv`).
- 4 new Claude memory entries written (codesign-provenance gotcha, swarm-report per-item verification rule, git ahead-count false-positive pattern, stale GH_TOKEN env override fix).
