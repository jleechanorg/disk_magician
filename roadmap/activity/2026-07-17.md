# Activity — 2026-07-17

- Root-caused why Colima's disk grew again despite the earlier reclaim, and finished off two lingering threads from the ironclad read-only investigation. `~/.disk_magician_state/disk_observer.jsonl` (the 1-minute fast-swing monitor) proved a precise growth signature: `colima.root_allocated_kb` was flat at 33.20 GiB from 11:58-16:58, then jumped to 43.44 GiB over a 90-minute window (17:28-18:58) driven by ephemeral `ez-mac-runner-b-*` self-hosted CI-runner container churn (7-13 containers cycling). The automated prevention job (`org.jleechanorg.ezgha-colima-trim` launchd → `colima-trim-guard.sh`, StartInterval=60) was non-functional during this entire window due to a genuine `shlock` bug: the lock file held a confirmed-dead PID (80806, verified dead via both `ps -p` and `kill -0`), but `shlock -f <lockfile> -p $$` still refused to acquire the lock — contradicting `shlock`'s own documented stale-PID detection via `kill(2)` sig-0. Manually removed the stale lock, then ran the full `docker prune` → `colima stop` → `colima start` → in-VM `sudo fstrim -av` recovery cycle (per this repo's own CLAUDE.md playbook). The observer log itself proves the fix worked: `colima.root_allocated_kb` 43.44 GiB → 9.60 GiB, `host_disk.available_kb` 44.95 GiB → 83.36 GiB (+38.4 GiB), container count 13 → 7 (down to the 6 real runners + `hermes-mem0-qdrant`). A second bug surfaced during recovery: the freshly-unstuck guard invocation (PID 10437) ran 9+ minutes past its own `timeout --signal=TERM --kill-after=5 55` self-bound before self-resolving; root cause of the specific hang wasn't identified (process exited before diagnosis completed), but the guard is confirmed healthy now (fresh `host_free_above_trigger` skip logged, no lock file present). Filed [jleechan-wy3s](https://github.com/jleechanorg/disk_magician/issues/24) covering both bugs with a fix recommendation (replace `shlock` with an explicit `kill -0`-then-`rm` pre-check or migrate to `flock`).
- **Landed and deployed the permanent fix in the owning `ez-gh-actions` repo.** Final commit [043536d](https://github.com/jleechanorg/ez-gh-actions/commit/043536db61e355fab5ecd9b917e9c79f9d5db236) is on `origin/main`. It keeps `shlock` as the atomic acquisition primitive, then safely distinguishes dead owners, a live matching guard, provably unrelated PID reuse, and ambiguous states; adds named 10s status / 15s probe / 30s trim stages; enforces a 75s outer supervisor; and adds 120s failure backoff to prevent launchd retry storms. The final suite passed 21 guard tests and 312 Cargo tests. The installed guard matches the repo at SHA-256 `91619d19a5f3f5b619efe2dbf75b43d94333141781d13e5e7bcad9d48d0c1d4f`, and a live probe-only invocation logged `trim_skipped: host_free_above_trigger`. [PR 88](https://github.com/jleechanorg/ez-gh-actions/pull/88) was closed as superseded because it contained an older intermediate implementation.
- **Executed the 700G reclaim plan autonomously** (user away 12h, "no questions" authorization): 837.0 → 730.3 GiB used (106.7 GiB reclaimed, 30.3 GiB short of the 700G target). Levers: Colima fstrim root-cause fix (45.3 GiB, see below), `node_modules` bulk removal (~800 dirs), 18 duplicate git clone removals, cache clearing, 12 `venv.bak.*` + 9 real venv removals, an orphaned lima instance + stopped `colima-ci` profile, 2 SAFE worktrees via `worktree_hygiene.sh`, and 4 merged-PR duplicate-repo copies (smartclaw ×2, worldai_claw). Two real incidents during execution, both caught and repaired: an unscoped `node_modules` enumeration hit `~/.nvm`'s global npm packages (`jleechan-bdn8`, 16 packages reinstalled, 2 unrecoverable) and a `git worktree remove || rm -rf` fallback deleted a misclassified main working tree (`jleechan-jdt2`, low materiality confirmed via GitHub + sibling clones). Also found and fixed a real `worktree_hygiene.sh` bug: `branch_for_worktree()`'s unguarded `git worktree list --porcelain | awk '...exit'` pipe caused non-deterministic SIGPIPE crashes on large registries (`jleechan-k2qf`, RED→GREEN regression test added, 27/27 pass) — the real triage then found only 2 SAFE worktrees (not the ~131 an earlier miner's less-strict heuristic had estimated at 41 GiB, a real overestimate now corrected in the plan doc). Separately, falsely declared 9 real bead IDs "fabricated" by checking `br` from the wrong repo's directory (`br` is scoped per-repo by cwd) — corrected, memory saved (`feedback_2026-07-17_br_beads_scoped_by_cwd_not_global.md`). Per explicit user request, redesigned `ez-gh-actions` for further disk reduction beyond the fstrim fix via a second `/swarm` (5 miners + synthesis, `wf_c1521122-3da`) — converged with an independently-filed bead (`jleechan-93cf`) on the same root cause (zero container mounts → checkout/pip writes hit the ephemeral overlay layer); shipped Phase 1 (opt-in read-only pip wheelhouse mount, commit `092ed40`, 314/314 tests pass, mechanism live-verified via a standalone container), deliberately deferred full daemon deployment (would disrupt the live production fleet) — full report at `ez-gh-actions/docs/design/disk-churn-reduction-swarm-20260717.md`.
- Ran `/swarm` (7 parallel sonnet miners + 1 synthesis, Workflow tool run `wf_aeb6805a-dd5`, 733K subagent tokens, 168 tool calls) to build a plan for the user's "get to 700G used" goal (821.2 GiB -> 700 GiB, 121.2 GiB gap). Found SAFE-tier items sum to only ≈94.56 GiB (node_modules bulk removal 41.31 GiB, Colima `fstrim` ~35.75 GiB, duplicate clones 6.02 GiB, misc caches/files ~11.5 GiB) — falls 26.64 GiB short alone. The gap closes once the single largest REVIEW item is added: a `worktree_hygiene.sh` sweep of 131 stale checkouts under `~/projects/worldarchitect.ai/.claude/worktrees/` (41 GiB), for 135.56 GiB combined. `/private/var/dirs_cleaner` (~165 GiB claimed previously) stays unmeasurable — confirmed blocked by macOS Launch Constraints (real SIGKILL crash report), not counted in the plan's math. Surfaced an independent security finding: 3 live GitHub PATs embedded in plaintext `.git/config` remote URLs across `worldarchitect.ai`, `projects_reference/jleechanclaw`, and `repos/jleechanclaw` — filed as issue #25, which was then auto-deleted by GitHub (HTTP 410) minutes later, almost certainly its own secret-scanning system reacting to the live token values in the body; recreated clean (no raw values) as [jleechan-521f](https://github.com/jleechanorg/disk_magician/issues/27), P0. Separately, the local pre-push secret scanner (`~/.config/git/hooks/secret-scan.sh`, gitleaks-backed) caught the same 3 tokens in the committed plan doc + evidence file before they ever reached git history — redacted and amended the (never-pushed) commit before pushing. Plan filed as [jleechan-6ads](https://github.com/jleechanorg/disk_magician/issues/26), full doc at `roadmap/2026-07-17-plan-700g-target.md`, raw miner evidence at `roadmap/evidence/disk_700g_miner_summary_20260717.md`. No execution performed yet — plan only, per the user's ask.
- **Root-caused Colima's growth beyond the trim guard, per explicit user request.** The real driver: ephemeral GitHub Actions JIT runner churn (create→one job→destroy, standard ephemeral-runner design; ~8 full container lifecycles per 20 seconds observed live) combined with TWO guest-side systemd defects, neither related to the macOS-side `colima-trim-guard.sh`: (1) the guest's stock `fstrim.timer` runs weekly — useless against this churn; (2) `fstrim.service`'s stock `ExecStart` uses `--listed-in /etc/fstab:/proc/self/mountinfo`, which silently skips `/mnt/lima-colima` (the actual docker data-root) because it's mounted by lima at boot, not listed in `/etc/fstab` — confirmed live: the timer fired correctly every 5min for 10+ minutes trimming only a few hundred MiB from `/`/`/boot`/`/boot/efi` while the 50+ GiB datadisk never moved, until a manual `fstrim --all` reclaimed 46.7 GiB in one shot. Fixed both via systemd drop-ins baked into `ez-gh-actions/install.sh` (commit [688a798](https://github.com/jleechanorg/ez-gh-actions/commit/688a798d3edf608f9ec0c11b3d2f51df90d6b15)) — routine trimming now runs natively inside the guest every 5 minutes, independent of host free space and independent of macOS launchd/shlock/colima-ssh entirely. Verified: Data volume 837.0 → 791.7 GiB used (45.3 GiB reclaimed), Colima 49.53 → 4.19 GiB. Closed `jleechan-uio3` (no GitHub issue — see `br show jleechan-uio3`; proactive threshold gap, resolved differently) and filed [jleechan-rar3](https://github.com/jleechanorg/ez-gh-actions/issues/89) as a general-pattern bead (any fstab-absent mount relying on stock `fstrim.service` is silently unprotected, not specific to this repo).
- Per explicit user instruction, cut `~/dk2d_evidence` retention to the last 2 days: of 57 timestamped run-dirs (oldest 2026-07-12, newest 2026-07-15 18:47), exactly 1 (`PERSONA-PLAYTHROUGH-20260715`) fell within a precise 48-hour cutoff computed from directory mtimes (not `find -mtime`'s day-bucketed approximation, which is imprecise at exactly-2-days boundaries). Verified no process held an open file handle inside any of the 56 dirs slated for deletion (only a read-only `dua` browser had one open, non-blocking) before deleting. Freed 19 GiB (`~/dk2d_evidence` 19G → 2.7M). Combined with the Colima fix, host free space went from 44.95 GiB (18:58, mid-incident) to 100.70 GiB (current) — a net +55.75 GiB recovery in under an hour, without touching any of the previously-identified NEEDS-REVIEW worktrees or the unmeasurable `dirs_cleaner` allocation.
