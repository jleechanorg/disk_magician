# Activity — 2026-07-14

- **Disk pressure recheck (READ-ONLY)** — Free space reached **5.8 GiB** around 21:00Z. The early Colima total of ~186 GiB was wrong because it added sparse files' apparent capacities and aliases; later allocated-byte measurements were about 22.4 GiB for `~/.colima` and 20.6 GiB for the active datadisk at 21:40Z, growing thereafter under runner churn. Snapshot v2's 472 GiB residual and +81.7 GiB residual delta describe unmeasured/accounting space, not physical bytes allocated or reclaimed. Live Docker events and 45-second `df`/`du` samples establish repeated one-shot ezgha runner replacement as a rapid Colima growth mechanism. Hermes and Codex database sizes remain separate retention concerns; no protected state was deleted.
- **Disk pressure roll-forward #2 and causal correction** — Free space later rose **5.8 GiB → ~76–79 GiB**, while snapshot coverage rose to 59.3% and residual fell to 345.7 GiB. The residual change came from measurement reclassification and did not free physical blocks. The only pressure sweep completed earlier at 19:45Z and the host-disk guardian reclaimed zero bytes during the recovery window, so neither caused the later recovery. A reboot at 21:13:33Z directly brackets the largest recovery and is consistent with release of deleted-but-open files, but the responsible process/file was not captured; that causal gap remains open under `jleechan-w5is`/`jleechan-tbe3`. Frontier-BFS integration was already shipped; its first scheduled launchd run had not occurred at this point. See `roadmap/nextsteps-2026-07-12-disk-magician-root-cause.md` for the canonical correction and gated follow-ups.
