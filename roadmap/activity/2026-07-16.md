# Activity — 2026-07-16

- Reclaimed ~38.8 GiB toward the user's "another 100GB" request via fully verified, reversible deletions (git-ignored/untracked cache dirs across worldarchitect.ai + 8 other repos, worktrees with merged PRs and zero local changes, worktrees whose commits were already captured on other remote branches). Zero data loss. Ran a 4-lane `/swarm` sweep (dark-factory, `~/.hermes`, ez-gh-actions+merge_train+disk_magician, worldarchitect.ai 87-candidate review); the unscoped full `scripts/worktree_hygiene.sh` run hung on 300+ sequential real `git push`+`gh pr list` calls (second time this has happened), worked around by scoping network checks to only the candidates where the answer could change the verdict. Session crashed (real reboot, `/tmp` wiped) and was resumed cleanly via JSONL transcript continuity + `/cmux-goal` restart. Diagnosed the post-crash 111.4GiB->88.0GiB regrowth as the same already-tracked `~/projects`/`.worktrees` AO-churn pattern (cross-referenced onto existing bead `jleechan-mf2b` rather than re-diagnosing); the structural fix is `jleechan-bd-vgyk-7f08` (AO lifecycle teardown), not more manual sweeps. Resolved `~/.pyenv/versions/3.11.10` (3.9GiB) as load-bearing for a live `devpi-server` daemon, not orphaned. Filed 9 beads for genuinely orphaned non-trivial work found during triage. Full detail: [nextsteps-2026-07-12-disk-magician-root-cause.md](../nextsteps-2026-07-12-disk-magician-root-cause.md) (2026-07-16 roll-forward section).

- Fixed `jleechan-q912` (`scripts/worktree_hygiene.sh`'s full-run hang on 300+ candidates) in an isolated worktree via `/newb` + `EnterWorktree`. Root-caused 3 stacked bugs, not just the originally-suspected sequential-network-calls issue: `-not -path` still descending into excluded venv/node_modules dirs (switched to `-prune`), per-file `stat` spawning (batched via `-exec +`), and a genuine SIGPIPE/`pipefail` crash bug confirmed present in the pre-fix committed script too (guarded with `|| true`). The `-prune` fix also incidentally resolved `jleechan-20gm` (the `.git` pointer file exclusion gap). Added a network-skip optimization to `triage_candidate` (skip real `git push`+`gh pr list` when local git state alone already determines the verdict). 26/26 script tests + 43/43 full suite pass. `/advice` reviewed (Opus high-confidence + research-confirmed idiom; secondo unavailable, expired auth) before merge. PR [jleechanorg/disk_magician#20](https://github.com/jleechanorg/disk_magician/pull/20) merged (SHA `46c75cd`), deployed to production via `uv tool install`, byte-diff-verified against the deployed tree.
