# Activity — 2026-07-15

- Sustained multi-hour root-cause session (two `/sidekick` teammates + one ultracode Workflow adversarial fan-out, self-enforced via `/cmux-goal`). Confirmed two disk-swing mechanisms with direct/live evidence (OS-reboot fd-release for the flagship 5.8GiB→76GiB recovery; Colima wedge→forced-restart→in-VM-trim, live-caught with byte-level datadisk shrinkage). Completed a rigorous single-pass disk accounting (834.65GiB used, 620.77GiB directly measured, 213.9GiB gap fully named as TCC/SIP-protected paths, not hand-waved — MobileSync backups called out as the likely largest single piece). Shipped and deployed a `dua-cli` fallback in `disk_snapshot.sh` for the intermittent-null measurement bug. Freed real bytes: Colima wedge recovery (+22GiB), `~/projects_reference` build-artifact purge (28.3GB→11GB, zero data loss), small Hermes-session and tmp-scratch sweeps. Found and filed a live credential leak (`jleechan-rx6v`, two plaintext GitHub PATs in a log file — still needs user rotation). Blocked on MobileSync deletion pending user granting Full Disk Access to `cmux`. Full detail: [nextsteps-2026-07-12-disk-magician-root-cause.md](../nextsteps-2026-07-12-disk-magician-root-cause.md) (2026-07-15 roll-forward section).

- Reconciled the complete 931.840 GiB physical SSD with parallel APFS, Data, home, and non-home scans. The new coherent breakdown names 821.124 GiB Data, 570.645 GiB home, 81.926 GiB other readable Data paths, and a 168.552 GiB attribution residual. Live checks refuted Time Machine/Data snapshots as the residual source; protected paths and frontier limits remain. Created `jleechan-rvqz` to make the default first-use diagnostic run three lanes concurrently: whole-disk top-down accounting, coverage-validated snapshot deltas, and safety-gated quick wins/outliers. Authorized cleanup reclaimed only 116 KiB plus guest-only trim, reinforcing that cleanup candidates are not a substitute for total accounting. Full detail: [nextsteps-2026-07-12-disk-magician-root-cause.md](../nextsteps-2026-07-12-disk-magician-root-cause.md) (full-SSD accounting roll-forward).

- Ran `/up` for the three-lane disk-diagnosis rule across Claude, Codex, Gemini, Cursor, and Hermes policy files. Labeled [disk_magician issue 18](https://github.com/jleechanorg/disk_magician/issues/18) for auto-factory, but verified it was not adopted: the live intake polls only `jleechanorg/worldarchitect.ai`. Recorded the BLOCKED state and connected it to [dark-factory issue 280](https://github.com/jleechanorg/dark-factory/issues/280) / [PR 283](https://github.com/jleechanorg/dark-factory/pull/283) rather than bypassing factory policy with inline coding.
