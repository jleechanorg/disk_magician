# Activity — 2026-09-01

- Disk Magician attribution hardening: released 0.2.82 (intermediate) FDA proof safety, recorded a fresh partial snapshot rather than a false complete ledger, and identified the root-owned immutable-runner requirement. Created `disk_magician-4y6` follow-up evidence and `disk_magician-q6h` for machine-specific catalog leakage. See [nextsteps-2026-09-01-full-attribution.md](../nextsteps-2026-09-01-full-attribution.md).
- Diagnosed CI run 33461832299: resolved `test_disk_audit_topdown.sh` FDA status assertion under clean runner environments where probe paths are missing (`no_targets`), corrected `test_safety_lib.sh` 7-day default assertion, updated `test_history_diff_dispatch.sh` and `test_state_repo_e2e.sh` FDA metadata fixtures. Verified 40/40 shell tests and 220 python tests pass at the time of this diagnosis (later grew to 226 after the /advice-driven fixes below).
- Resolved `disk_magician-q6h` / GitHub issue #56: cleaned `DEFAULT_HOT_DIRS` and `config.json.template` of host-specific entries, updated safety template docs, added catalog purity tests in `tests/test_disk_observer.py`, synced package tree, and bumped version to `0.2.83` (intermediate).
- `/advice` primary-pair review (Codex + Opus) of PR #57 found and fixed a symlink/TOCTOU privilege-escalation gap in `install_root_frontier_runner.sh`, a frontier-tiebreak completeness bug, and a self-inconsistent `test_state_repo_e2e.sh` fixture; final package version for this PR is `0.2.85`, final local suite is 226/226 Python tests and 40/40 shell test files.

